server:
  port: 6868
eureka:
  client:
    register-with-eureka: false  # 是否往Eureka注册
    fetch-registry: false  # 是否从Eureka获取注册信息
    service-url:   # 暴露Eureka的注册地址
      defaultZone: http://127.0.0.1:${server.port}/eureka
